{
  "origin": { // コピー元
    "pid": (ポータルID: number),
    "apiKey": (APIキー: string),
    "host": (ホスト: string 例：www.example.com)
  },
  "target": { // コピー先
    "pid": (ポータルID: number),
    "apiKey": (APIキー: string),
    "host": (ホスト: string 例：example.hs-sites.com)
  }
}